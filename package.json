{
    "name": "kanbanboard-root",
    "version": "1.0.0",
    "private": true,
    "workspaces": [
        "backend",
        "frontend"
    ],
    "scripts": {
        "build": "npm run build -w frontend && mkdir -p public && cp -r frontend/dist/frontend/browser/* public/",
        "test": "echo \"Error: no test specified\" && exit 1"
    }
}